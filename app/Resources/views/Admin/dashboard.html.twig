{% extends "SonataAdminBundle:Core:dashboard.html.twig" %}

{# override javascript includes #}
{% block javascripts %}
    {{ parent() }}
    <script defer src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">
        jQuery(document).ready(function() {
            google.load('visualization', '1', {packages:['corechart'], 'language': '{{ app.request.locale }}'});
        });
    </script>
{% endblock %}
