{% extends ':Frontend:base.html.twig' %}

{% block title %}{{ event.title }} · Activitat · {{ parent() }}{% endblock %}

{% block main %}
    <div class="container container-main">
        <div class="row">
            <div class="col-xs-12">
                <h2 class="text-center fresh-talent-ad">Event detail</h2>
                <h5>{{ event.date | date('d/m/Y') }}</h5>
                <h5>{{ event.title }}</h5>
                <h5>{{ event.type }}</h5>
                <h5>{{ event.description }}</h5>
            </div>
        </div>
    </div>
{% endblock %}
